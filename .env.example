# Backend (FastAPI) — none required for local V1; audio analysis runs
# entirely locally with no external API keys.

# Frontend (Next.js) — where the browser should find the backend API.
# Copy this into frontend/.env.local
NEXT_PUBLIC_API_BASE=http://localhost:8000
